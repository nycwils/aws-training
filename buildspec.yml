version: 0.2

phases:
  install:
    commands:
      - echo installation command goes here
  pre_build:
    commands:
      - echo Nothing to do in the pre_build phase...
  build:
    commands:
      - echo Build started on `date`
  post_build:
    commands:
      - echo Build completed on `date`
artifacts:
    commands:
      - echo artifacts file path goes here